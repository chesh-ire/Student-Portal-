// Example: Add event listener to the "Set Goals" button
const setGoalsButton = document.querySelector('.profile button');

setGoalsButton.addEventListener('click', () => {
  // Logic to handle clicking the button, maybe open a modal or redirect
  console.log('Set Goals button clicked!');
});